{
  "name": "mock-data",
  "version": "1.0.0",
  "description": "Mock data for use in app examples",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "TIMING=1 eslint . --fix",
    "type-check": "tsc --noEmit -p ."
  },
  "dependencies": {
    "eslint-config-custom": "*",
    "zod": "^3.20.6"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "eslint": "^8.34.0"
  }
}
